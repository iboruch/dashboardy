# Screenshots

Add real project screenshots here before publishing the repository or sharing it with recruiters.

Recommended files:

- `dashboard-light.png` - dashboard in light mode
- `dashboard-dark.png` - dashboard in dark mode
- `settings.png` - settings and demo status messaging
- `mobile.png` - responsive mobile layout

The root README already references these local paths. Until images are added, the section explains that screenshots are pending instead of using external placeholder services.
